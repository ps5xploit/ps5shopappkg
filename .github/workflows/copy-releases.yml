name: Copy Releases from Source Repo

on:
  workflow_dispatch:

jobs:
  copy-releases:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout destination repo
        uses: actions/checkout@v4

      - name: Install GitHub CLI (for uploads only)
        run: |
          echo "📦 Instalando GitHub CLI..."
          curl -L -o gh.tar.gz https://github.com/cli/cli/releases/latest/download/gh_2.95.0_linux_amd64.tar.gz
          tar -xzf gh.tar.gz
          sudo mv gh_*_linux_amd64/bin/gh /usr/local/bin/
          sudo chmod +x /usr/local/bin/gh
          gh --version

      - name: Copy releases using GitHub API
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
          SOURCE_REPO: "eternumplay/pkstg"
          DEST_REPO: "ps5xploit/ps5shopappkg"
          ALREADY_COPIED: "PPSA05132 PPSA31246 PPSA01474 PPSA02530 PPSA28101 kenia PPSA21783 PPSA13195 PPSA20560 UP0700-PPSA06774_00-ARMOREDCORE60000 UP0700-PPSA04220_00-JASBRMAINGAME000 UP0177-PPSA03831_00-SONICRANGERS0000 UP0700-PPSA04220_00-JASBRMAINGAME000 UP0177-PPSA03831_00-SONICRANGERS0000 UP0031-PPSA02721_00-NEPREVERSEGAME01 PPSA07230 Aragami.2.CUSA23443.PS4 PPSA10887 UP6856-PPSA05424_00-WWZTHEGAME2021US UP6312-PPSA25258_00-0173149685884705 UP5223-PPSA05721_00-WYTCHWOODPS5SIEA UP4735-PPSA02121_00-CRISTALES0000000 UP4363-PPSA04170_00-EVILGENIUS200000 UP4293-PPSA02527_00-KITARIAFABLES000 UP3165-PPSA02604_00-0024231166387186 UP2011-PPSA04181_00-0922527761026563 "
        run: |
          export GH_TOKEN="$GH_TOKEN"
          gh auth status
          
          echo "📦 Obteniendo releases de $SOURCE_REPO usando API..."
          
          # Obtener la lista de releases
          RELEASES=$(curl -s -H "Authorization: token $GH_TOKEN" \
            "https://api.github.com/repos/$SOURCE_REPO/releases?per_page=100")
          
          echo "📋 Total releases: $(echo "$RELEASES" | jq '. | length')"
          
          echo "$RELEASES" | jq -c '.[]' | while read -r release; do
            TAG_NAME=$(echo "$release" | jq -r '.tag_name')
            RELEASE_NAME=$(echo "$release" | jq -r '.name')
            IS_PRERELEASE=$(echo "$release" | jq -r '.prerelease')
            
            # Verificar si el juego ya está en la lista de copiados
            if echo "$ALREADY_COPIED" | grep -q "$TAG_NAME"; then
              echo "  ⏭️  Saltando: $TAG_NAME (ya copiado)"
              continue
            fi
            
            # Verificar si el release ya existe en destino
            if gh release view "$TAG_NAME" --repo "$DEST_REPO" &>/dev/null; then
              echo "  ⏭️  Release $TAG_NAME ya existe. Saltando."
              continue
            fi
            
            echo "📦 Procesando: $RELEASE_NAME ($TAG_NAME)"
            
            echo "  📝 Creando release $TAG_NAME..."
            if [ "$IS_PRERELEASE" = "true" ]; then
              gh release create "$TAG_NAME" --title "$RELEASE_NAME" --prerelease --repo "$DEST_REPO" --notes "Copiado desde ps5shopappkg"
            else
              gh release create "$TAG_NAME" --title "$RELEASE_NAME" --repo "$DEST_REPO" --notes "Copiado desde ps5shopappkg"
            fi
            
            # Obtener las URLs de los assets del release
            ASSETS=$(echo "$release" | jq -r '.assets[].browser_download_url')
            
            for ASSET_URL in $ASSETS; do
              FILENAME=$(basename "$ASSET_URL")
              echo "    📥 Descargando: $FILENAME"
              curl -L -o "$FILENAME" "$ASSET_URL"
              echo "    📤 Subiendo: $FILENAME"
              gh release upload "$TAG_NAME" "$FILENAME" --repo "$DEST_REPO" --clobber
              rm "$FILENAME"
              echo "    ✅ $FILENAME copiado."
            done
            echo "  ✅ $TAG_NAME copiado correctamente."
          done
          
          echo "🎉 ¡Proceso completado!"
